#include <stdio.h>
void main () {
int a;
float d;
char c, name[40];
printf("Please enter your data\n");
scanf("%d %f %c %s", &a, &d, &c, name);
printf("\nThe values accepted are"), %d, %f, %c, %s, a d, c, name;
}
